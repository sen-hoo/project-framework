设计模式
1.装饰模式:
    特点：少用继承，就是多用组合，就是不同子类组合一起实现。而且不改变对外接口性。
        在编译期才知道传入具体功能
2.代理模式

    代理模式和装饰模式的区别：关注点不同，代理模式主要是隐藏一个实际对象的具体信息，控制对象的访问。而装饰模式在于对象的动态添加方法上，而不改变原有类的实现。
    因此，当使用代理模式的时候，我们常常在一个代理类中创建一个对象的实例。并且，当我们使用装饰器模 式的时候，我们通常的做法是将原始对象作为一个参数传给装饰者的构造器。
3.观察着模式（发布订阅模式）
    就是